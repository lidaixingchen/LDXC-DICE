# AcuDice 骰子系统 - 深度 Bug 排查报告

**生成日期**: 2026-04-29  
**项目版本**: 2.0.0  
**总览**: 共发现 **约 120+ 个问题**，涵盖逻辑错误、类型安全缺陷、性能问题、安全漏洞等。  
**修复日期**: 2026-04-30  
**修复状态**: 已修复 97 个，误报 3 个，未修复 23 个（主要为低危/架构级问题）

---

## 目录

1. [严重级别说明](#1-严重级别说明)
2. [中危 Bug（边界与设计问题）](#4-中危-bug边界与设计问题)
3. [低危与代码异味](#5-低危与代码异味)

---

## 1. 严重级别说明

| 级别 | 定义 |
|------|------|
| **严重** | 导致崩溃、数据损坏、功能完全不可用 |
| **高危** | 产生错误计算结果，功能行为严重偏离预期 |
| **中危** | 边界条件处理不当，设计脆弱，潜在运行时异常 |
| **低危** | 代码异味、维护性问题、性能损耗 |

---

## 4. 中危 Bug（边界与设计问题）

### 4.1 核心引擎
| # | 文件 | 问题 | 状态 |
|---|------|------|------|
| 4 | `attribute-manager.ts:88` | 基于 `rowIndex` 的偏移存取设计脆弱 | ⬜ 未修复（架构级） |
| 8 | `debug-console.ts:352` | `eval(code)` 调试命令存在安全风险 | ⬜ 未修复（设计决策） |

### 4.2 数据层
| # | 文件 | 问题 | 状态 |
|---|------|------|------|
| 11 | 多处 | 无跨 tab 的 localStorage 同步 | ⬜ 未修复（架构级） |

### 4.3 工具与地图
| # | 文件 | 问题 | 状态 |
|---|------|------|------|
| 1 | `performance.ts:43` | `lastRenderTime` 在回调执行前更新，时间计算偏差 | ⬜ 未修复（设计选择，非 bug） |
| 10 | `avatar-manager.ts:264-271` | 头像存储为 base64 数据 URL，localStorage 配额易超 | ⬜ 未修复（架构级） |

### 4.4 Vue 前端
| # | 文件 | 问题 | 状态 |
|---|------|------|------|
| 2 | `index.ts:231-234` | `settingsManager.onChange` 回调从未注销，内存泄漏 | ❌ 误报：全局回调应保持存活 |
| 15 | `InteractionManager.vue:135` | 以部分更新（仅 `enabled` 字段）当作完整规则更新 | ❌ 误报：`updateRuleInPreset` 使用 spread 保留原字段 |

## 5. 低危与代码异味

### 5.3 维护性问题

- 中文错误消息贯穿代码库 → ⬜ 未修复（国际化需求）
- 多处 `as any` / `as unknown as` 类型断言 → ⬜ 部分修复

## 6. 综合统计

| 文件领域 | 严重 | 高危 | 中危 | 低危 |
|----------|------|------|------|------|
| 核心引擎 (core/) | 3 | 8 | 13 | 5 |
| 数据层 (data/ + adapters/ + presets/) | 3 | 5 | 12 | 6 |
| 工具 (utils/) | 1 | 3 | 6 | 3 |
| 地图系统 (map/) | 1 | 4 | 14 | 3 |
| Vue 前端 (ui/) | 4 | 7 | 18 | 8 |
| **合计** | **12** | **27** | **63** | **25** |

**总计发现问题数量**: 约 127 个

### 修复统计

| 类别 | 已修复 | 误报 | 未修复 | 合计 |
|------|--------|------|--------|------|
| 严重 Bug | 9 | 1 | 0 | 10 |
| 高危 Bug | 24 | 1 | 0 | 25 |
| 中危 Bug | 52 | 1 | 10 | 63 |
| 低危/代码异味 | 12 | 0 | 13 | 25 |
| **合计** | **97** | **3** | **23** | **123** |

**修复率**: 78.9%（97/123）  
**误报率**: 2.4%（3/123）  
**高危以上修复率**: 100%（33/33，含误报 2 个）

---

## 7. 优先修复建议

### ~~第一优先级（功能完全不可用或数据损坏）~~ ✅ 全部已修复
1. ~~API 初始化锁死~~ ✅
2. ~~复数骰子括号/优先级丢失~~ ✅
3. ~~COC7 96-99 无结果~~ ✅
4. ~~`with()` 严格模式抛出~~ ✅
5. ~~`LazyLoader` 名称冲突~~ ✅
6. ~~`<OpposedCheckPanel>` 缺少 import~~ ✅
7. ~~`runInSaveQueue` 竞争条件~~ ✅
8. ~~`MapPanel.vue` 除以零~~ ✅

### ~~第二优先级（严重逻辑错误）~~ ✅ 全部已修复
1. ~~`p` 修饰符映射 + `applyPenetrate` 死代码~~ ✅
2. ~~`evaluateCondition` 不支持复合条件~~ ✅
3. ~~`EffectEngine` 调用不存在的方法~~ ❌ 误报
4. ~~COC7 `extreme_success` 死代码~~ ✅
5. ~~事件监听器无法移除~~ ✅
6. ~~地图渲染器每帧重新加载图片~~ ✅
7. ~~各 `new Function()` 注入风险~~ ✅

### ~~第三优先级（边界与质量）~~ ✅ 全部已修复

1. ~~各 `compareVersion` 重复定义提取~~ ✅
2. ~~`isEqual` 使用 `JSON.stringify` 替换~~ ✅
3. ~~各处 `Set`/`Map` 响应式兼容性~~ ✅
4. ~~各处 `deepClone` 一致性~~ ✅ 已修复：优先使用 structuredClone
5. ~~各处 localStorage 键名命名空间~~ ✅ 已修复
6. ~~模块级副作用~~ ✅ 已修复

### 第四优先级（剩余项）

1. `avatar-manager` base64 配额问题 → ⬜ 架构级
2. 跨 tab localStorage 同步 → ⬜ 架构级
3. 中文错误消息国际化 → ⬜ 需单独规划
4. `debug-console:eval` → ⬜ 设计决策（已加 500 字符限制）
5. `performance.ts` lastRenderTime 时间偏差 → ⬜ 设计选择
6. `attribute-manager.ts` rowIndex 偏移存取 → ⬜ 架构级
